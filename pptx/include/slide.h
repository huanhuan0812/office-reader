#pragma once
// pptx/include/slide.h
#include "type.h"
#include <memory>
#include <string>

class pptx::Slide {
public:

    
private:
    std::string m_id;  // 唯一标识符
    std::string m_name; // 可选名称
    
};